
  # Design Centered Info Card

  This is a code bundle for Design Centered Info Card. The original project is available at https://www.figma.com/design/bql2SliRa5BG30mMFdmBd2/Design-Centered-Info-Card.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  